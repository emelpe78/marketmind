# Docker Compose: Host-Ordner für persistente Daten (Bind-Mount → /app/data im Container)
# Relativ zum Repo-Root oder absolut (z. B. Nextcloud-Ordner passend zu MM_DATABASE_DOCKER)
MARKETMIND_DATA_DIR=./data
